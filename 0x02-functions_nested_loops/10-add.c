#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @a: the first number
 * @b: the second number
 * Return: the result of add.
 */

int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
